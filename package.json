{
  "name": "quicktake-200",
  "type": "module",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:cli": "bun run --cwd ./packages/cli dev",
    "dev:app": "bun run --cwd ./packages/app dev"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/w3c-web-serial": "^1.0.8"
  }
}
